<p align="center">
  <img src="https://raw.githubusercontent.com/Abojawdat/iPhone-Duo-package-/main/doc/logo.svg" width="72" alt="شعار duo_dynamic_sizing">
</p>

<div dir="rtl">

<h1 align="center">لماذا تعمل duo_dynamic_sizing بهذه الطريقة</h1>

<p align="center">
  <a href="https://github.com/Abojawdat/iPhone-Duo-package-/blob/main/DESIGN.md">English</a> · <b>العربية</b> · <a href="https://github.com/Abojawdat/iPhone-Duo-package-/blob/main/README.ar.md">العودة إلى الصفحة الرئيسية</a>
</p>

كل قرار في هذه الصفحة جرى التحقق منه مقابل الأرقام الحقيقية لهاتف iPhone Duo وإرشادات Apple نفسها. ومعظم هذه القرارات حلّت محل فكرة سابقة لم تصمد عند الاختبار، لذلك نذكر الأفكار المستبعدة أيضاً.

---

## ١. الأحجام لا تُكبَّر أبداً

**القرار:** يبقى النص بحجم 16 نقطة بحجم 16 نقطة على كل شاشة. وعندما تتسع المساحة يكسب التخطيط لوحات وأعمدة إضافية، لا عناصر أكبر.

**السبب:** ترسم Apple شاشتي Duo بالحجم الفعلي نفسه للنقطة.

| | بكسل في البوصة | بكسل لكل نقطة | نقطة في البوصة |
| --- | --- | --- | --- |
| الشاشة الخارجية | 460 | 3.0 | 153.3 |
| الشاشة الداخلية | 430 | 2.807 (تُرسم بدقة 2007 على لوحة بدقة 1878) | 153.2 |

النقطة لها الحجم نفسه على الشاشتين، فالشاشة الداخلية هي ببساطة مساحة أكبر. ولو كُبّرت الأحجام حسب العرض لتضاعف كل شيء بنسبة 951 ÷ 466 = 2.04، وقفز النص من 16 إلى 33 نقطة لحظة فتح الهاتف.

**ما استبعدناه:** التكبير حسب العرض على طريقة screenutil. الإصدار الأول من هذا المشروع (0.0.1) فعل ذلك تماماً، والبحث في أرقام الجهاز الحقيقية هو ما أسقط هذه الفكرة.

## ٢. كل شيء يأتي من `MediaQuery`

**القرار:** تُبنى `context.duo` فقط من `MediaQuery.sizeOf` و`paddingOf` و`displayFeaturesOf` و`devicePixelRatioOf`.

**السبب:**
- **النافذة هي ما يملكه تطبيقك فعلاً.** في العرض المقسّم أو Stage Manager أو نافذة سطح المكتب تكون النافذة أصغر من الجهاز، ويجب أن يتبع التخطيط النافذة.
- **إعادة البناء دقيقة.** هذه الجوانب الأربعة تعيد بناء العنصر عند الطي والتدوير والعرض المقسّم، ولا تعيد بناءه أبداً عند ظهور لوحة المفاتيح أو تغيير حجم الخط. وهناك اختبار يثبت ذلك.
- **يمكن محاكاتها.** بما أن الحزمة كلها تقرأ من `MediaQuery`، يستطيع `DuoSimulator` محاكاة أي جهاز في الاختبارات وفي التطبيق أثناء تشغيله.

**ما استبعدناه:**
- **`View.of(context).display`:** ليست تفاعلية، وتحذر Apple من أن الشاشة الرئيسية غامضة على جهاز بشاشتين.
- **اسم طراز الجهاز:** يحتاج إلى كود أصلي، ولا يخبرك بشيء عن العرض المقسّم.
- **فحص اتجاه الجهاز:** الشاشة الداخلية في Duo تتجاهل قفل الاتجاه.

## ٣. يُتعرّف على Duo من مقاساته الدقيقة

**القرار:** على iOS وبكثافة 3x، تُعدّ النوافذ التالية نوافذ iPhone Duo، بهامش تسامح ±1 نقطة:

| النافذة | الوضع |
| --- | --- |
| 466 × 678 (أو أضيق مع إصدارات SDK أقدم) | `closedPortrait` |
| 678 × 466 | `closedLandscape` |
| 951 × 669 (أو حتى 75% منها) | `openLandscape` |
| 669 × 951 | `openPortrait` |
| أي جزء أضيق من الشاشة الداخلية | `splitView` |

**السبب:** لا يوجد iPhone آخر بهذه المقاسات بكثافة 3x. أجهزة iPad بكثافة 2x، وأجهزة Android وسطح المكتب لا تعرّف نفسها كنظام iOS أبداً. لذلك لا يُخلط أبداً بين نافذة Stage Manager على iPad بمقاس 466 × 678 وبين Duo.

**ما استبعدناه:**
- **التخمين حسب نسبة العرض إلى الارتفاع:** أجهزة Android القابلة للطي لها نسب مشابهة، مما يسبب نتائج خاطئة.
- **معرّف الجهاز من `utsname` (`iPhone19,4`):** يحتاج إلى كود أصلي، ويصف الجهاز لا النافذة.

## ٤. لوحتان ابتداءً من 600 × 480

**القرار:** تعني `isExpanded` أن العرض لا يقل عن 600 نقطة **وأن** الارتفاع لا يقل عن 480 نقطة.

**السبب:** الرقم 600 هو العرض المتوسط في Material 3. وإضافة شرط الارتفاع 480 تعيد إنتاج فئات الحجم الخاصة بـ Apple على Duo بدقة تامة:

| نافذة Duo | فئة الحجم لدى Apple | عرض 600؟ | ارتفاع 480؟ | `isExpanded` |
| --- | --- | --- | --- | --- |
| مطوي 466 × 678 | ضيقة | لا | نعم | لا ✓ |
| مطوي أفقياً 678 × 466 | ضيقة | نعم | لا | لا ✓ |
| مفتوح 951 × 669 | عادية | نعم | نعم | نعم ✓ |
| مفتوح عمودياً 669 × 951 | عادية | نعم | نعم | نعم ✓ |
| العرض المقسّم 475 × 669 | ضيقة | لا | نعم | لا ✓ |

**ما استبعدناه:** حدّ يعتمد على العرض وحده. كان سيعطي Duo المطوي أفقياً لوحتين ضيقتين، بينما تعامل Apple هذه الوضعية كوضعية ضيقة.

## ٥. الحالة تبقى بالنقل لا بإعادة البناء

**القرار:** عندما يتغير شكل التخطيط تُنقل العناصر إلى مكانها الجديد مع الاحتفاظ بالعنصر نفسه، باستخدام آلية نقل `GlobalKey` في Flutter. ويفعل ذلك كل من `DuoKeep` و`DuoSplit` و`DuoListDetail` وجسم الصفحة في `DuoNavigationScaffold` و`DuoSimulator`.

**السبب:** يحتفظ التطبيق بكل ما يلاحظه المستخدم: موضع التمرير، والنص الذي يكتبه، والتحديد، والحركات الجارية. وتضع Apple الاستمرارية عند الطي ضمن المتطلبات، لا ضمن الكماليات.

**ما استبعدناه:**
- **مطالبة المطورين برفع كل الحالة إلى أعلى الشجرة:** عبء كبير وسهل الخطأ.
- **`PageStorage` وحده:** لا يحفظ إلا موضع التمرير.

## ٦. التقسيم على خط الطي، والعكس حسب الاتجاه

**القرار:**
- **مكان التقسيم:** يقسم `DuoSplit` تماماً على خط الطي، أي عند 475.5 نقطة على الشاشة الداخلية لـ Duo، أو عند المفصل الذي يبلّغ عنه Android.
- **الصناديق المزاحة:** يقيس موقعه بنفسه بعد كل إطار، فيبقى محاذياً حتى تحت شريط التطبيق أو بجانب شريط التنقل.
- **من اليمين إلى اليسار:** تتبادل اللوحتان مكانيهما في التطبيقات العربية، لكن التقسيم نفسه لا يتحرك.

**السبب:** المفصل يحدد مكان التقسيم، واتجاه القراءة يحدد أي لوحة تأتي أولاً. والخلط بين الأمرين يضع المحتوى فوق خط الطي.

**ما استبعدناه:**
- **نصف الصندوق المحلي:** يخطئ خط الطي كلما كان الصندوق مزاحاً.
- **عكس الشكل الهندسي في RTL:** يُبعد التقسيم عن خط الطي.

## ٧. التنقل حيث يضعه iOS 27

**القرار:**
- **على Duo:** يكون شريط التنقل على الجهة اليمنى الفعلية، بجانب الجزيرة الديناميكية، في كل اللغات، لأن Apple تحاذي الأشرطة العمودية مع العتاد.
- **في العرض المقسّم:** يحصل التطبيق الأيسر على شريطه على الحافة اليسرى.
- **مفتوح وعمودي:** هي الوضعية الوحيدة ذات الشريط السفلي.
- **الأجهزة الأخرى:** تُطبق قواعد Material 3 (شريط جانبي من 600 نقطة، على جهة البداية).
- **`railSide`:** يمكن للتطبيقات التي تفضّل اتجاه القراءة في كل مكان أن تختاره.

**المتانة (كل بند أدناه جاء من اختبارات الحالات الحدّية):**
- **حجم الخط:** تسميات الشريط محدودة بـ 1.3x، وهو الحد نفسه الذي يستخدمه `NavigationBar` في Flutter. كان النص بحجم 3x يتجاوز Duo المطوي بـ 47 بكسل.
- **التمرير:** يتمرر الشريط عندما لا تتسع العناصر. كانت سبعة تبويبات تتجاوز ارتفاع 386 نقطة على Duo المطوي أفقياً.
- **التسميات الطويلة** تُختصر بثلاث نقاط.
- **العرض:** لا يأخذ الشريط أكثر من 30% من النافذة. ويحتفظ بعرضه الطبيعي 80 نقطة عبر `IntrinsicWidth` داخل هذا الحد. وقد التقطت اختبارات الصور المرجعية (golden) النسخة الأولى وهي تمط الشريط إلى 140 نقطة.

## ٨. ملاءمة ذكية للوسائط عند 15%

**القرار:** لا يقص `DuoMediaFit.smart` (cover) إلا إذا كان القص يخفي 15% أو أقل من الوسائط، وإلا أضاف أشرطة (contain).

**السبب:** هذه هي الأرقام الحقيقية للشاشة الداخلية في Duo بمقاس 951 × 669:

| الفيديو | يخفيه cover | يتركه contain فارغاً | يختار smart |
| --- | --- | --- | --- |
| 16:9 | 20% | 20% | contain |
| 4:3 | 6% | 6% | cover |
| 3:2 | 5% | 5% | cover |

تملأ الصور ومعظم فيديوهات مواقع التواصل الشاشة، ويحتفظ الفيديو السينمائي بحوافه.

**ما استبعدناه:** القص الدائم الذي يقطع الترجمة والوجوه، والأشرطة الدائمة التي تهدر خُمس شاشة ثمنها 1,999 دولاراً.

## ٩. Dart فقط، دون كود أصلي

**القرار:** لا تحتوي الحزمة على قنوات منصة ولا على كود iOS أو Android ولا على CocoaPods.

**السبب:**
- **تعمل اليوم** مع أي إصدار من Flutter بدءاً من 3.41، ومع أي إصدار من Xcode، وعلى أي منصة بما فيها الويب وسطح المكتب.
- **نظام iOS لا يمرر بيانات المفصل إلى Flutter حتى الآن** ([flutter#192515](https://github.com/flutter/flutter/issues/192515)). وعندما يحدث ذلك ستحملها `displayFeatures`، وستلتقطها هذه الحزمة دون أي تغيير.
- **Apple نفسها تقول إن بيانات المفصل مخصصة للمؤثرات** لا للتخطيط. التخطيط يحتاج إلى النافذة، والنافذة موجودة أصلاً في Dart.

**ما استبعدناه:** كتابة إضافة أصلية. كانت ستحتاج إلى Xcode 27.1، وستعمل على iOS فقط، وتحمل عبء صيانة الكود الأصلي. وهناك حزم مثل [`foldable`](https://pub.dev/packages/foldable) تغطي زاوية المفصل الحية، ويمكن دمجها مع هذه الحزمة بسهولة.

## ١٠. لا انهيار أبداً عند المدخلات الخاطئة

**القرار:** كل ما قد يفعله المطور عن غير قصد يجب أن يتراجع بهدوء دون أن ينهار التطبيق.

| الحالة | السلوك |
| --- | --- |
| `DuoSplit` داخل عرض قابل للتمرير أو `Column` غير محدود | لوحة واحدة |
| `DuoMedia` بنسبة 0 أو سالبة أو NaN أو لا نهائية | تنبيه في وضع التطوير، وملء الصندوق في وضع الإصدار |
| نافذة 0 × 0 أو 150 × 150 | يعمل، بلوحة واحدة |
| هاتف ثلاثي الطي بمفصلين | يقسم عند الطية الأولى |
| خط طي خارج النافذة | يُتجاهل |
| دون `MaterialApp` أو `Navigator` | يستمر `DuoListDetail` في العمل |

كل هذه الحالات موجودة في [`test/edge_cases_test.dart`](test/edge_cases_test.dart)، وكل إصلاح لخطأ يحصل على حالة اختبار هناك.

## ١١. مختبرة دون الجهاز

**القرار:** يمكن اختبار كل شيء قبل صدور iPhone Duo، ودون محاكي Xcode 27.1.

| الاختبار | ما يتحقق منه |
| --- | --- |
| 97 اختباراً للوحدات وعناصر الواجهة والحالات الحدّية والصور المرجعية | كل وضع ووضعية وطيّة واتجاه ومكان لشريط التنقل، إضافة إلى صورة مرجعية لكل وضعية |
| 10 اختبارات تكامل على تطبيق macOS الحقيقي | اللمس والطي والعرض المقسّم والعربية وتغيير حجم النافذة حياً على المحرك الحقيقي |
| أداة التصيير | الحركات في هذه الصفحة إطارات حقيقية من التطبيق التجريبي |

## ١٢. ما تركناه عمداً

- **زاوية المفصل على iOS.** محرك Flutter لا يوفرها بعد.
- **الأشرطة العمودية الأصلية** (`toolbarVerticalBehavior` و`ArrangementView`). لا يوجد لها مقابل في Flutter، لذلك يحاكي `DuoNavigationScaffold` أماكنها.
- **التكبير التلقائي.** راجع القرار الأول.
- **واجهات النوافذ المتعددة.** هي خارج نطاق حزمة تخطيط.

</div>

---

<p align="center">
  <sub>MIT © Mohammad Othman · <a href="https://github.com/Abojawdat/iPhone-Duo-package-">github.com/Abojawdat/iPhone-Duo-package-</a></sub>
</p>
